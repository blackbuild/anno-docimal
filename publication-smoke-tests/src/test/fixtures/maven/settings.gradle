dependencyResolutionManagement {
    repositories {
        maven {
            url = uri('%%REPOSITORY%%')
            metadataSources {
                mavenPom()
                artifact()
            }
        }
    }
}

rootProject.name = 'annodocimal-maven-publication-consumer'
