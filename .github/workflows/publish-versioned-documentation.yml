name: Publish protected versioned documentation

on:
  workflow_dispatch:
    inputs:
      revision:
        description: Exact lowercase source commit SHA
        required: true
        type: string
      version:
        description: Exact final or RC version
        required: true
        type: string
      status:
        description: Documentation lifecycle status
        required: true
        type: choice
        options: [pending, public-rc, current, archived]
      release_stage:
        description: Required candidate or final stage for pending evidence; omit otherwise
        required: false
        type: string
      branding_manifest:
        description: Versioned branding manifest; required for product snapshots and omitted for legacy archives
        required: false
        default: docs/branding/annodocimal-current.json
        type: string
      successor_of:
        description: Exact RC version superseded by this final release, after release proof
        required: false
        type: string
      alias_proof:
        description: HTTPS reference to #45-recorded public-artifact proof; required before a public route advances an alias
        required: false
        type: string
      deploy:
        description: Publish the validated artifact after protected-environment approval
        required: true
        default: false
        type: boolean

permissions:
  contents: read

jobs:
  validate-and-render:
    runs-on: ubuntu-latest
    outputs:
      revision: ${{ steps.validate.outputs.revision }}
      version: ${{ steps.validate.outputs.version }}
      status: ${{ steps.validate.outputs.status }}
      render_path: ${{ steps.validate.outputs.render_path }}
      snapshot_path: ${{ steps.validate.outputs.snapshot_path }}
    steps:
      - id: validate
        shell: bash
        env:
          REVISION: ${{ inputs.revision }}
          VERSION: ${{ inputs.version }}
          STATUS: ${{ inputs.status }}
          RELEASE_STAGE: ${{ inputs.release_stage }}
          SUCCESSOR_OF: ${{ inputs.successor_of }}
          ALIAS_PROOF: ${{ inputs.alias_proof }}
          DEPLOY: ${{ inputs.deploy }}
        run: |
          [[ "$REVISION" =~ ^[0-9a-f]{40}$ ]]
          [[ "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+(-rc\.[1-9][0-9]*)?$ ]]
          [[ "$DEPLOY" == true || "$DEPLOY" == false ]]
          case "$STATUS" in
            pending)
              [[ -z "$ALIAS_PROOF" ]]
              [[ "$RELEASE_STAGE" == candidate || "$RELEASE_STAGE" == final ]]
              if [[ "$RELEASE_STAGE" == candidate ]]; then [[ "$VERSION" == *-rc.* ]]; else [[ "$VERSION" != *-rc.* ]]; fi
              ;;
            public-rc)
              [[ -z "$RELEASE_STAGE" ]]
              [[ "$VERSION" == *-rc.* ]]
              if [[ "$DEPLOY" == true ]]; then [[ "$ALIAS_PROOF" =~ ^https://[^[:space:]]+$ ]]; else [[ -z "$ALIAS_PROOF" ]]; fi
              ;;
            current|archived)
              [[ -z "$RELEASE_STAGE" ]]
              [[ "$VERSION" != *-rc.* ]]
              if [[ "$STATUS" == current && "$DEPLOY" == true ]]; then [[ "$ALIAS_PROOF" =~ ^https://[^[:space:]]+$ ]]; else [[ -z "$ALIAS_PROOF" ]]; fi
              ;;
            *) exit 1 ;;
          esac
          if [[ -n "$SUCCESSOR_OF" ]]; then
            [[ "$STATUS" == current ]]
            [[ "$SUCCESSOR_OF" =~ ^[0-9]+\.[0-9]+\.[0-9]+-rc\.[1-9][0-9]*$ ]]
          fi
          echo "revision=$REVISION" >> "$GITHUB_OUTPUT"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "status=$STATUS" >> "$GITHUB_OUTPUT"
          if [[ "$STATUS" == pending ]]; then
            echo "render_path=$VERSION" >> "$GITHUB_OUTPUT"
            echo "snapshot_path=pending/$VERSION/$REVISION" >> "$GITHUB_OUTPUT"
          else
            echo "render_path=$VERSION" >> "$GITHUB_OUTPUT"
            echo "snapshot_path=$VERSION" >> "$GITHUB_OUTPUT"
          fi
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.revision }}
          path: source-object
          fetch-depth: 0
      - name: Confirm exact source commit
        shell: bash
        env:
          REVISION: ${{ inputs.revision }}
          VERSION: ${{ inputs.version }}
          STATUS: ${{ inputs.status }}
        run: |
          test "$(git -C source-object rev-parse HEAD)" = "$REVISION"
          if [[ "$STATUS" != pending ]]; then
            test "$(git -C source-object tag --points-at HEAD "v$VERSION")" = "v$VERSION"
          fi
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 17
      - uses: gradle/actions/setup-gradle@v4
      - name: Generate Javadocs from the exact source commit
        if: inputs.status != 'archived'
        working-directory: source-object
        run: ./gradlew javadoc
      - name: Render immutable site artifact
        shell: bash
        env:
          REVISION: ${{ inputs.revision }}
          VERSION: ${{ inputs.version }}
          STATUS: ${{ inputs.status }}
          RELEASE_STAGE: ${{ inputs.release_stage }}
          BRANDING_MANIFEST: ${{ inputs.branding_manifest }}
          SUCCESSOR_OF: ${{ inputs.successor_of }}
        run: |
          arguments=(
            -PdocumentationRevision="$REVISION"
            -PdocumentationRendererRevision="$(git rev-parse HEAD)"
            -PdocumentationVersion="$VERSION"
            -PdocumentationStatus="$STATUS"
            -PdocumentationObjectDirectory="$GITHUB_WORKSPACE/source-object"
            -PdocumentationOutputDirectory="$GITHUB_WORKSPACE/build/versioned-documentation"
          )
          if [[ -n "$RELEASE_STAGE" ]]; then arguments+=(-PdocumentationReleaseStage="$RELEASE_STAGE"); fi
          if [[ "$STATUS" != archived ]]; then
            arguments+=(
              -PdocumentationBrandingManifest="$BRANDING_MANIFEST"
              -PdocumentationCurrentBrandingManifest=docs/branding/annodocimal-current.json
            )
          fi
          if [[ -n "$SUCCESSOR_OF" ]]; then arguments+=(-PdocumentationSuccessorOf="$SUCCESSOR_OF"); fi
          ./gradlew verifyRenderedVersionedDocumentationSite \
            "${arguments[@]}"
      - uses: actions/upload-artifact@v4
        with:
          name: versioned-documentation
          path: build/versioned-documentation
          if-no-files-found: error

  write-canonical-immutable-snapshot:
    if: inputs.deploy
    needs: validate-and-render
    runs-on: ubuntu-latest
    environment:
      name: annodocimal-pages-writer
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Require the requested source to be current master
        shell: bash
        env:
          REVISION: ${{ needs.validate-and-render.outputs.revision }}
          VERSION: ${{ needs.validate-and-render.outputs.version }}
          STATUS: ${{ needs.validate-and-render.outputs.status }}
        run: |
          set -euo pipefail
          test "$(git rev-parse "$REVISION^{commit}")" = "$REVISION"
          if [[ "$STATUS" == archived ]]; then
            test "$(git tag --points-at "$REVISION" "v$VERSION")" = "v$VERSION"
          else
            master_revision="$(git ls-remote --exit-code origin refs/heads/master | awk '{print $1}')"
            test "$master_revision" = "$REVISION"
          fi
      - uses: actions/download-artifact@v4
        with:
          name: versioned-documentation
          path: rendered
      - name: Assert the staged artifact is bound to the requested source
        shell: bash
        env:
          REVISION: ${{ needs.validate-and-render.outputs.revision }}
          VERSION: ${{ needs.validate-and-render.outputs.version }}
          STATUS: ${{ needs.validate-and-render.outputs.status }}
          RENDER_PATH: ${{ needs.validate-and-render.outputs.render_path }}
        run: |
          set -euo pipefail
          manifest="rendered/$RENDER_PATH/source-manifest.json"
          test -f "$manifest"
          jq -e --arg revision "$REVISION" --arg version "$VERSION" --arg status "$STATUS" \
            '.source.revision == $revision and .documentation.version == $version and .documentation.status == $status' \
            "$manifest" >/dev/null
      - uses: actions/checkout@v4
        with:
          ref: gh-pages
          path: pages
          fetch-depth: 1
          persist-credentials: false
      - name: Add one previously absent exact snapshot
        shell: bash
        env:
          VERSION: ${{ needs.validate-and-render.outputs.version }}
          STATUS: ${{ needs.validate-and-render.outputs.status }}
          RENDER_PATH: ${{ needs.validate-and-render.outputs.render_path }}
          SNAPSHOT_PATH: ${{ needs.validate-and-render.outputs.snapshot_path }}
        run: |
          test ! -e "pages/$SNAPSHOT_PATH"
          mkdir -p pages
          touch pages/.nojekyll
          mkdir -p "pages/$(dirname "$SNAPSHOT_PATH")"
          mv "rendered/$RENDER_PATH" "pages/$SNAPSHOT_PATH"

          write_alias() {
            local alias="$1"
            local target="$2"
            local label="$3"
            local explanation="$4"
            mkdir -p "pages/$alias"
            {
              printf '%s\n' '<!doctype html>' '<html lang="en"><head><meta charset="utf-8">'
              printf '<meta http-equiv="refresh" content="0; url=/anno-docimal/%s/">\n' "$target"
              printf '<title>%s</title></head><body><h1>%s</h1>\n' "$label" "$label"
              printf '<p>%s <a href="/anno-docimal/%s/">Open immutable documentation %s</a>.</p>\n' "$explanation" "$target" "$target"
              printf '%s\n' '</body></html>'
            } > "pages/$alias/index.html"
          }

          case "$STATUS" in
            public-rc)
              write_alias preview "$VERSION" 'Preview documentation (proof-gated)' 'This alias names one publicly proven release candidate.'
              ;;
            current)
              maintained_line="${VERSION%.*}"
              write_alias stable "$VERSION" 'Stable documentation (proof-gated)' 'This alias names one publicly proven final release.'
              write_alias "$maintained_line" "$VERSION" "Maintained $maintained_line documentation (proof-gated)" 'This alias names the publicly proven final release for its maintained line.'
              ;;
            archived)
              mkdir -p pages/archive
              {
                printf '%s\n' '<!doctype html>' '<html lang="en"><head><meta charset="utf-8"><title>Documentation archive</title></head><body>'
                printf '%s\n' '<h1>Documentation archive</h1><p>Historical snapshots remain at their immutable version routes.</p><ul>'
                while IFS= read -r archived_version; do
                  printf '<li><a href="/anno-docimal/%s/">%s</a></li>\n' "$archived_version" "$archived_version"
                done < <(
                  for manifest in pages/*/source-manifest.json; do
                    [[ -f "$manifest" ]] || continue
                    if [[ "$(jq -r '.documentation.status' "$manifest")" == archived ]]; then
                      jq -r '.documentation.version' "$manifest"
                    fi
                  done | sort -V
                )
                printf '%s\n' '</ul></body></html>'
              } > pages/archive/index.html
              ;;
          esac
          if [[ "$STATUS" != pending ]]; then
            mkdir -p pages/status
            cp -R rendered/status/. pages/status/
          fi
          git -C pages config user.name github-actions[bot]
          git -C pages config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git -C pages add "$SNAPSHOT_PATH"
          git -C pages add .nojekyll
          if [[ "$STATUS" != pending ]]; then git -C pages add status; fi
          if [[ "$STATUS" == public-rc ]]; then git -C pages add preview; fi
          if [[ "$STATUS" == current ]]; then git -C pages add stable "$maintained_line"; fi
          if [[ "$STATUS" == archived ]]; then git -C pages add archive; fi
          git -C pages commit -m "Publish immutable documentation snapshot $SNAPSHOT_PATH"
          echo "pushed_commit=$(git -C pages rev-parse HEAD)" >> "$GITHUB_ENV"
      - name: Require protected writer credentials
        shell: bash
        env:
          PAGES_WRITER_APP_ID: ${{ secrets.PAGES_WRITER_APP_ID }}
          PAGES_WRITER_APP_PRIVATE_KEY: ${{ secrets.PAGES_WRITER_APP_PRIVATE_KEY }}
        run: |
          set -euo pipefail
          test -n "$PAGES_WRITER_APP_ID"
          test -n "$PAGES_WRITER_APP_PRIVATE_KEY"
      - name: Mint the dedicated Pages writer token
        id: pages-writer-token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.PAGES_WRITER_APP_ID }}
          private-key: ${{ secrets.PAGES_WRITER_APP_PRIVATE_KEY }}
      - name: Write the canonical immutable snapshot
        shell: bash
        env:
          PAGES_WRITER_TOKEN: ${{ steps.pages-writer-token.outputs.token }}
        run: |
          set -euo pipefail
          test -n "$PAGES_WRITER_TOKEN"
          git -C pages push "https://x-access-token:${PAGES_WRITER_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" HEAD:gh-pages
      - uses: actions/checkout@v4
        with:
          ref: gh-pages
          path: written-pages
          fetch-depth: 1
          persist-credentials: false
      - name: Read back the canonical commit and source manifest
        shell: bash
        env:
          PUSHED_COMMIT: ${{ env.pushed_commit }}
          REVISION: ${{ needs.validate-and-render.outputs.revision }}
          VERSION: ${{ needs.validate-and-render.outputs.version }}
          STATUS: ${{ needs.validate-and-render.outputs.status }}
          SNAPSHOT_PATH: ${{ needs.validate-and-render.outputs.snapshot_path }}
          RENDER_PATH: ${{ needs.validate-and-render.outputs.render_path }}
        run: |
          set -euo pipefail
          test "$(git -C written-pages rev-parse HEAD)" = "$PUSHED_COMMIT"
          staged_manifest="rendered/$RENDER_PATH/source-manifest.json"
          written_manifest="written-pages/$SNAPSHOT_PATH/source-manifest.json"
          test -f "$written_manifest"
          cmp -- "$staged_manifest" "$written_manifest"
          jq -e --arg revision "$REVISION" --arg version "$VERSION" --arg status "$STATUS" \
            '.source.revision == $revision and .documentation.version == $version and .documentation.status == $status' \
            "$written_manifest" >/dev/null
